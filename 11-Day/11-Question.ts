//Greetins: Start with the array you used in Exresice 11, but instead of just printing each should be the same,
// But each message should be personalized with the person's name.

let listOfArray: string[] = ["Salman", "Faizan", "Asif"];

for (const listArray of listOfArray) {
    console.log(`Hey Good Moring ${listArray}`);
}